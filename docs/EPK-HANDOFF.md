# HYENAZ MAFIA MEDIA — EPK Page Handoff

> **Working branch for the heavy-detail EPK build.**
> Emmond is authoring the EPK content in Claude Cowork and will commit it here
> as Markdown. Claude Code then arranges it into the `/epk` page in proper order,
> on-brand (crimson = artist/edge, gold = business/premium; tabular numerals;
> `next/image`; existing `@theme` tokens — no hardcoded hex; respect
> `prefers-reduced-motion`).

## Drop your content below (replace this section)

_Paste/commit the EPK details here — bio, long-form story, accolades, stats,
press quotes, photos, links, etc. Note any ordering or emphasis you want._

## Asset notes
- Any new images go in `public/` (e.g. `public/epk/`) and are referenced by path.
- Streaming / social links: Spotify, Apple, Instagram (@spitta_p), etc.
